<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        title Meldocia
    body
        include partials/header
        div
            p Plop
        //button.roue_button(type="button" onclick="display(0)") description
        //button.roue_button(type="button" onclick="display(1)") skills
        //button.roue_button(type="button" onclick="display(2)") portfolio
        //button.roue_button(type="button" onclick="display(3)") contact
        include partials/footer
        
script.
    var plop = 2;
