import cssArticles from '/model/css_articles.json' with { type: 'json' };

generateCssArticles(cssArticles);

function generateCssArticles(cssArticles) {
    const skillsElement = document.querySelector("#cssarticles");
}
